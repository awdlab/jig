{
  "name": "@ngneers/controls-custom-types",
  "version": "0.0.1",
  "scripts": {
    "lint": "pnpm eslint .",
    "prebuild": "pnpm rimraf ./dist",
    "postbuild": "tsx ./tools/post-build.ts",
    "build": "pnpm prebuild && tsc -p ./tsconfig.lib.json --outDir ./dist && pnpm postbuild"
  },
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "~5.9.3"
  },
  "sideEffects": true
}
