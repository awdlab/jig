import { createControlTemplate } from '@ngneers/controls-themes/api';

export const scrollerControlTemplate = createControlTemplate({
  scope: 'scroller',
  classNames: ['', 'scrollarea', 'sticky'],
});
