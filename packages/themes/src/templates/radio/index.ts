import { createControlTemplate } from '@awdlab/jig-themes/api';

export const radioControlTemplate = createControlTemplate({
  scope: 'radio',
  classNames: ['root', 'circle', 'circle-checked', 'dot', 'label'],
});
