import { createControlTemplate } from '@awdlab/jig-themes/api';

export const otpControlTemplate = createControlTemplate({
  scope: 'otp',
  classNames: ['root', 'box', 'invalid'],
  dependencies: [],
});
