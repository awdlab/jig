import { createControlTemplate } from '@ngneers/controls-themes/api';

export const inplaceControlTemplate = createControlTemplate({
  scope: 'inplace',
  classNames: ['root', 'content', 'display', 'disabled'],
});
