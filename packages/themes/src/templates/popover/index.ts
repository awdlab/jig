import { createControlTemplate } from '@ngneers/controls-themes/api';

export const popoverControlTemplate = createControlTemplate({
  scope: 'popover',
  classNames: ['root', 'content', 'content-closing', 'anim-in', 'anim-out'],
});
