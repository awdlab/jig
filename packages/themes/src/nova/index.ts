import { createTheme } from '@ngneers/controls-themes/api';
import { coral } from '@ngneers/controls-themes/nova/base';
import { button } from '@ngneers/controls-themes/nova/button';

export const novaCoral = createTheme('Nova Coral', [coral, button]);
