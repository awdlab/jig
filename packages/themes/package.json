{
  "name": "@ngneers/controls-themes",
  "version": "0.0.1",
  "scripts": {
    "lint": "pnpm eslint .",
    "prebuild": "pnpm rimraf ./dist",
    "postbuild": "tsx ./tools/post-build.ts",
    "build": "pnpm prebuild && tsgo -p ./tsconfig.lib.json --outDir ./dist && pnpm postbuild"
  },
  "type": "module",
  "dependencies": {
    "tslib": "^2.8.1",
    "@ngneers/controls-custom-types": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.1.2",
    "typescript": "catalog:"
  },
  "sideEffects": false
}
