import { ALL_COMPONENT_PAGES } from '../../docs/components';

/** Controls advertised on the startpage — derived from the component doc pages so it stays in sync. */
export const CONTROL_COUNT = ALL_COMPONENT_PAGES.length;
