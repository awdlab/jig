import '@ngneers/controls-custom-types';

declare module '@ngneers/controls-custom-types' {
  export interface NgnCustomTypes {
    icon: `img/icons/${'bars' | 'github' | 'code' | 'copy' | 'user' | 'sort_up' | 'sort_down' | 'sort'}.svg`;
  }
}
