import test from '@playwright/test';

test.fixme('basic toast creation', async ({ page }, testInfo) => {
  /**
   * Implement once we can create component logic in tests.
   * Toasts need to be created via function, currently not possible in our e2e tests.
   */
});
